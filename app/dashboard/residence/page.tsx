'use client'

import ResidenceForm from '../../../components/ResidenceForm'

/* The landlord's residence, reached from Profile and the dashboard prompt. */
export default function ResidencePage() {
  return <ResidenceForm onSaved={() => {}} />
}
